<h1>Categoria: {{categorias.nome}}</h1>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>Titulo: {{titulo}}</h4>
            <p>Descrição: {{descricao}}</p>

            <a href="/postagens/{{slug}}">
                <button class="btn btn-success">
                    Leia mais
                </button>
            </a>

            <hr>

            <small>Categoria: {{categoria}}</small> <br>
            <small>Data de publicação: {{#formatDate}} {{data}} {{/formatDate}}</small>
        </div>
    </div>
{{else}}
    <h4>Nenhuma postagem registrada nessa categoria</h4>
{{/each}}